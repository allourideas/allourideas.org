<%= t('items.email.hello') %>

<%= t('items.email.photocracy_someone_has_flagged', :question_url => "http://#{@photocracy ? PHOTOCRACY_HOST : HOST}/#{@earl.name}") %>

<%= t('items.email.provided_explanation_is', :object => @object_type) %>

<%= @explanation %>

<%= t('items.email.photocracy_we_have_auto_deactivated') %>

<%= @choice_url %>

<%= t('items.email.if_you_dont_want', :object => @object_type) %>

<%= @photocracy ? t('items.email.see_current_results_photocracy') : t('items.email.see_current_results') %>
<%= "http://#{@photocracy ? PHOTOCRACY_HOST : HOST}/#{@earl.name}" %>

<%= t('items.email.thanks') %>
<%= render :partial => 'idea_mailer/signature' %>
