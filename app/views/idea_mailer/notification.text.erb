<%= t('items.email.hello') %>

<%= t('items.email.someone_has_uploaded') %> <%= @photocracy ? t('items.email.a_photo') : t('items.email.the_idea', :idea_text => @choice_text ) %> <%= t('items.email.to_your_question') + " '#{@question_name}'" %>

<%= t('items.email.if_you_want_to_activate', :object_type => @object_type) %>
<%= @choice_url %>

<%= t('items.email.if_you_dont_want', :object_type => @object_type) %>

<% if @similar.length > 0 %>
<%= t('items.email.existing_similar_ideas') %>
  <% @similar.each do |similar_url| %>
    <%= similar_url %>
  <% end %>
<% end %>

<%= @photocracy ? t('items.email.see_current_results_photocracy') : t('items.email.see_current_results') %>
<%= "http://#{@photocracy ? APP_CONFIG[:PHOTOCRACY_HOST] : APP_CONFIG[:HOST]}/#{@earl.name}" %>

<%= t('items.email.thanks') %>
<%= render :partial => 'idea_mailer/signature' %>
